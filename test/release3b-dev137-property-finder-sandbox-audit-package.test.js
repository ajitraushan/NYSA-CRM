import test from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';import crypto from 'node:crypto';import {unzipSync} from 'fflate';
const base=new URL('../release-artifacts/release-3/r3b/',import.meta.url),read=name=>readFileSync(new URL(name,base)),text=name=>read(name).toString('utf8');
test('dev.137 sandbox audit hotfix is checksum-bound migration-neutral and portable',()=>{const name='nysa-core-r3b-property-finder-sandbox-audit-hotfix-dev137.zip',zip=read(name),sha=crypto.createHash('sha256').update(zip).digest('hex'),manifest=JSON.parse(text('nysa-core-r3b-property-finder-sandbox-audit-hotfix-dev137.manifest.json')),entries=unzipSync(zip),names=Object.keys(entries);assert.equal(text('nysa-core-r3b-property-finder-sandbox-audit-hotfix-dev137.sha256.txt').trim(),`${sha}  ${name}`);assert.equal(manifest.packageSha256,sha);assert.equal(manifest.version,'2.1.0-dev.137');assert.equal(manifest.baselineVersion,'2.1.0-dev.136');assert.equal(manifest.latestMigration,'081_release3b_portal_permit_reconciliation.sql');assert.equal(manifest.migrationCount,81);assert.equal(manifest.migrationNeutral,true);for(const required of ['package.json','src/property-finder-sandbox.js','src/routes/property-finder-sandbox.js','release-artifacts/release-3/r3b/deploy-crm-test-r3b-dev137-property-finder-sandbox-audit-hotfix.sh','RUNTIME_MANIFEST.sha256','MANIFEST.sha256'])assert.ok(names.includes(required),required);for(const entry of names)assert.ok(!entry.includes('\\')&&!entry.startsWith('/')&&!entry.split('/').includes('..'),entry);const records=value=>Buffer.from(value).toString('utf8').trim().split(/\r?\n/).filter(Boolean).map(line=>({sha:line.slice(0,64),path:line.slice(66)}));for(const item of records(entries['MANIFEST.sha256']))assert.equal(crypto.createHash('sha256').update(entries[item.path]).digest('hex'),item.sha,item.path);for(const item of records(entries['RUNTIME_MANIFEST.sha256']))assert.equal(crypto.createHash('sha256').update(entries[item.path]).digest('hex'),item.sha,item.path);assert.equal(JSON.parse(Buffer.from(entries['package.json'])).version,'2.1.0-dev.137');});
test('dev.137 deployer enforces Broker audit binding and never calls Property Finder',()=>{const deploy=text('deploy-crm-test-r3b-dev137-property-finder-sandbox-audit-hotfix.sh');for(const marker of ['EXPECTED_VERSION=2.1.0-dev.137','PREVIOUS_VERSION=2.1.0-dev.136','EXPECTED_PACKAGE=nysa-core-r3b-property-finder-sandbox-audit-hotfix-dev137.zip','LATEST_MIGRATION=081_release3b_portal_permit_reconciliation.sql','governed Broker audit binding is missing','unsupported sandbox audit entity remains','Production snapshot changed','R2 clone snapshot changed'])assert.ok(deploy.includes(marker),marker);assert.doesNotMatch(deploy,/curl[^\n]*sandbox\.atlas\.propertyfinder\.com/);});
