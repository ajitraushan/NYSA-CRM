import test from 'node:test';import assert from 'node:assert/strict';import crypto from 'node:crypto';import fs from 'node:fs';import {unzipSync} from 'fflate';const root=new URL('../release-artifacts/release-3/r3b/',import.meta.url),read=n=>fs.readFileSync(new URL(n,root)),text=n=>read(n).toString('utf8');
test('dev.125 AI render-loop package is portable checksum-bound and migration-neutral',()=>{const name='nysa-core-r3b-ai-render-loop-hotfix-dev125.zip',zip=read(name),sha=crypto.createHash('sha256').update(zip).digest('hex'),manifest=JSON.parse(text('nysa-core-r3b-ai-render-loop-hotfix-dev125.manifest.json')),entries=unzipSync(zip),names=Object.keys(entries);assert.equal(text('nysa-core-r3b-ai-render-loop-hotfix-dev125.sha256.txt').trim(),`${sha}  ${name}`);assert.equal(manifest.packageSha256,sha);assert.equal(manifest.version,'2.1.0-dev.125');assert.equal(manifest.baselineVersion,'2.1.0-dev.124');assert.equal(manifest.migrationCount,78);assert.deepEqual(manifest.requirements,['R3B-REQUIREMENT-AI-63']);for(const required of ['package.json','public/app.js','src/ai-service.js','src/migrations/078_release3b_inventory_progressive_governance.sql','release-artifacts/release-3/r3b/deploy-crm-test-r3b-dev125-ai-render-loop-hotfix.sh','MANIFEST.sha256'])assert.ok(names.includes(required));const prior=unzipSync(read('nysa-core-r3b-ai-responsiveness-hotfix-dev124.zip'));for(const migration of ['075_release3b_governed_inventory_matching.sql','076_release3b_match_decisions_and_feedback.sql','077_release3b_requirement_confirmation.sql','078_release3b_inventory_progressive_governance.sql'])assert.deepEqual(Buffer.from(entries[`src/migrations/${migration}`]),Buffer.from(prior[`src/migrations/${migration}`]));});
test('dev.125 deployer is CRM-Test-only rerunnable and rollback-ready',()=>{const deploy=text('deploy-crm-test-r3b-dev125-ai-render-loop-hotfix.sh'),guide=text('RELEASE_3B_DEV125_AI_RENDER_LOOP_HOTFIX_DEPLOYMENT_AND_UAT.md');for(const marker of ['EXPECTED_ROOT=/home/nysareal/nysa-core-dashboard-dd6262a-stage','PRODUCTION_ROOT=/home/nysareal/nysa-crm','R2_CLONE_ROOT=/home/nysareal/nysa-r2-prod-clone','EXPECTED_VERSION=2.1.0-dev.125','PREVIOUS_VERSION=2.1.0-dev.124','expected installed dev.124 or dev.125','78|$LATEST_MIGRATION','pre-dev125.dump','pre-dev125-app.tar.gz'])assert.ok(deploy.includes(marker));assert.doesNotMatch(deploy,/dev124|dev\.123/);for(const marker of ['Files to upload','Focused UAT','Cancel generation','Rollback'])assert.ok(guide.includes(marker));});
