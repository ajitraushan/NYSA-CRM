import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import crypto from 'node:crypto';import {unzipSync} from 'fflate';
const base=new URL('../release-artifacts/release-3/r3b/',import.meta.url),read=name=>fs.readFileSync(new URL(name,base)),text=name=>read(name).toString('utf8');
test('dev.127 Opportunity authority package is portable checksum-bound and migration-neutral',()=>{const name='nysa-core-r3b-opportunity-authority-boundary-hotfix-dev127.zip',zip=read(name),sha=crypto.createHash('sha256').update(zip).digest('hex'),manifest=JSON.parse(text('nysa-core-r3b-opportunity-authority-boundary-hotfix-dev127.manifest.json')),entries=unzipSync(zip),names=Object.keys(entries);assert.equal(text('nysa-core-r3b-opportunity-authority-boundary-hotfix-dev127.sha256.txt').trim(),`${sha}  ${name}`);assert.equal(manifest.packageSha256,sha);assert.equal(manifest.version,'2.1.0-dev.127');assert.equal(manifest.baselineVersion,'2.1.0-dev.126');assert.equal(manifest.migrationCount,78);assert.deepEqual(manifest.requirements,['R3B-OPPORTUNITY-AUTHORITY-68']);for(const required of ['package.json','public/app.js','src/routes/opportunities.js','src/migrations/078_release3b_inventory_progressive_governance.sql','release-artifacts/release-3/r3b/deploy-crm-test-r3b-dev127-opportunity-authority-boundary-hotfix.sh','MANIFEST.sha256'])assert.ok(names.includes(required));const prior=unzipSync(read('nysa-core-r3b-opportunity-inventory-eligibility-hotfix-dev126.zip'));for(const migration of ['075_release3b_governed_inventory_matching.sql','076_release3b_match_decisions_and_feedback.sql','077_release3b_requirement_confirmation.sql','078_release3b_inventory_progressive_governance.sql'])assert.deepEqual(Buffer.from(entries[`src/migrations/${migration}`]),Buffer.from(prior[`src/migrations/${migration}`]));});
test('dev.127 deployer is CRM-Test-only rerunnable and rollback-ready',()=>{const deploy=text('deploy-crm-test-r3b-dev127-opportunity-authority-boundary-hotfix.sh'),guide=text('RELEASE_3B_DEV127_OPPORTUNITY_AUTHORITY_BOUNDARY_HOTFIX_DEPLOYMENT_AND_UAT.md');for(const marker of ['EXPECTED_ROOT=/home/nysareal/nysa-core-dashboard-dd6262a-stage','PRODUCTION_ROOT=/home/nysareal/nysa-crm','R2_CLONE_ROOT=/home/nysareal/nysa-r2-prod-clone','EXPECTED_VERSION=2.1.0-dev.127','PREVIOUS_VERSION=2.1.0-dev.126','expected installed dev.126 or dev.127','78|$LATEST_MIGRATION','pre-dev127.dump','pre-dev127-app.tar.gz'])assert.ok(deploy.includes(marker));assert.doesNotMatch(deploy,/dev125|dev\.125|opportunity-inventory-eligibility/);for(const marker of ['Files to upload','NYSA-INV-000040','buyer/tenant','seller/landlord','Rollback'])assert.ok(guide.includes(marker));});
