import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import crypto from 'node:crypto';
const artifact=name=>new URL(`../release-artifacts/release-3/consolidated/${name}`,import.meta.url);
test('dev.170 package is checksum-bound cumulative migration-neutral and CRM-Test-only',()=>{const name='nysa-core-consolidated-crm-test-dev170.zip',bytes=fs.readFileSync(artifact(name)),sha=crypto.createHash('sha256').update(bytes).digest('hex'),checksum=fs.readFileSync(artifact(name.replace('.zip','.sha256.txt')),'utf8').trim(),manifest=JSON.parse(fs.readFileSync(artifact(name.replace('.zip','.manifest.json')),'utf8'));assert.equal(checksum,`${sha}  ${name}`);assert.equal(manifest.packageSha256,sha);assert.equal(manifest.version,'2.1.0-dev.170');assert.equal(manifest.target,'CRM Test only');assert.equal(manifest.baselineVersion,'2.1.0-dev.169');assert.equal(manifest.migrationCount,107);assert.equal(manifest.latestMigration,'107_dev169_versioned_opportunity_stage_drafts.sql');assert.ok(manifest.requirements.includes('DEF-093/UAT-093'));assert.deepEqual(manifest.verification.ordinary,{total:1243,passed:1213,failed:0,skippedProtected:30});assert.deepEqual(manifest.verification.focusedLinkedFunctions,{total:26,passed:26,failed:0});});
test('dev.170 installer accepts only dev.169 migration-107 or exact rerun baseline',()=>{const script=fs.readFileSync(artifact('deploy-crm-test-consolidated-dev170.sh'),'utf8'),safetyBody=fs.readFileSync(artifact('deploy-crm-test-consolidated-dev162.sh'),'utf8');for(const marker of ['2.1.0-dev.170','2.1.0-dev.169','107_dev169_versioned_opportunity_stage_drafts.sql','CRM Test'])assert.ok(script.includes(marker));assert.match(safetyBody,/Production and R2 clone were not targeted/);assert.match(script,/for number in 101 102 103 104 105 106 107/);});
