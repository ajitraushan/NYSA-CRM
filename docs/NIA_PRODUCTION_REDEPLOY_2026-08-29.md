# NIA production redeployment — 2026-08-29

## Scope

- Force customers onto the corrected multi-select/Continue handler by changing the hard-coded `script.js` cache key from `20260815-multiselect1` to `20260829-nia-continue2`.
- Preserve the current NIA typography.
- Increase the desktop NIA drawer to a maximum width of `720px` and use the available viewport height.
- Keep the existing mobile layout unchanged below `701px`.

## Production files

1. `wp-content/themes/nysa-advisory-theme/functions.php`
2. `wp-content/themes/nysa-advisory-theme/prototype/styles.css`

## Rollback

- Restore the script cache key to `20260815-multiselect1`.
- Remove the final CSS block headed `NIA customer-facing desktop workspace — 20260829`.

## Verification

- Reload the public homepage with the new script URL.
- Restore/open the saved NIA journey at the confidence multi-select question.
- Select one option and verify that Continue is enabled.
- Confirm the desktop drawer is wider/taller and the existing font rules are unchanged.
- Confirm the mobile rule remains full-width.

## Completion evidence

- Production `functions.php`: old cache key count `0`; new `20260829-nia-continue2` key count `1`.
- Production stylesheet: desktop override marker count `1`.
- Public homepage after LiteSpeed purge serves `script.js?v=20260829-nia-continue2`.
- Public homepage serves refreshed `styles.css?v=1787993082`.
- Live desktop computed layout: drawer width `720px`, height `684px`, conversation height `530px` in the verification viewport.
- Live prompt typography remains `Inter, ui-sans-serif, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif` at the existing `12px` prompt scale.
- LiteSpeed reported: `Notified LiteSpeed Web Server to purge all LSCache entries.`

## Three-selection follow-up

- Corrected the confidence-question maximum-selection defect: Continue is now available for 1, 2, or 3 selected options.
- The handler removes the native `disabled` attribute whenever the multi-select has a valid non-empty selection; the click guard still rejects an empty selection.
- Advanced the production script cache key to `20260829-nia-continue3` and purged LiteSpeed again.
- Verified the public homepage serves `script.js?v=20260829-nia-continue3`.

## REM CRM handoff correction

- Confirmed the completed assessment was safely stored in Advisory V2 at 100%, but remained `awaiting_adapter` because that adapter targets NYSA CORE rather than the currently operational REM CRM.
- Replayed the saved assessment through the established `nysa/v1/ai-leads` integration. WordPress created lead `12` and marked the REM delivery `Queued`.
- Verified the replayed lead appears at the top of the live REM CRM Leads screen as a new, un-contacted website lead.
- Updated NIA completion sync to post an idempotent `nia_v2_<assessment_id>` lead to the existing REM integration after Advisory V2 saves successfully.
- Persisted a `remSynced` flag to avoid repeat delivery on restored summary screens.
- Changed **Finish & return to NYSA** to wait for the active CRM sync before clearing local journey state.
- Advanced the public cache key to `20260829-nia-rem1`; verified the public script contains the REM endpoint, `remSynced` protection, and the `Saving to CRM…` completion state.

## Viewport-safe navigation follow-up

- Confirmed from the customer screenshot that all three confidence choices were selected while the complete Back/Continue action row had been pushed below the visible drawer.
- Constrained the desktop conversation grid to `auto minmax(0, 1fr) auto`, made only the options row scroll when needed, and kept the action row inside the viewport.
- Preserved the current font, drawer width, selection styling, and mobile layout.
- Purged production cache and verified the public stylesheet advanced to `styles.css?v=1787995047`.

- Root-cause correction: the NIA conversation runs inside an iframe, so the earlier `.chat-drawer` descendant rules could not reach it. Added the viewport lock directly to `.advisor-embed .ai-conversation`, constrained it to `calc(100dvh - 175px)`, and kept `.ai-actions` in the final visible grid row.
- Purged again and verified the iframe-specific rule is publicly served in `styles.css?v=1787996348`.

## Embedded bundle cache correction

- Identified the remaining production defect: `prototype/investor-profile.html` still referenced the stale `script.js?v=20260813-interaction1` bundle, so Continue remained natively disabled even after a valid selection.
- Updated the embedded page to load `script.js?v=20260829-nia-rem2`.
- Added `nysa_release=20260829-nia-rem2` to the iframe URL generated by `prototype/inner-pages.js`, preventing the browser/CDN from reusing the old cached HTML shell.
- Purged LiteSpeed after both production changes.
- End-to-end verification confirmed the refreshed parent launcher (`inner-pages.js?ver=1787997076`) opens the versioned iframe and that the iframe loads `script.js?v=20260829-nia-rem2`.
- Verified the live handler explicitly removes the native `disabled` attribute for every non-empty multi-select state, including one, two, and three selections.
