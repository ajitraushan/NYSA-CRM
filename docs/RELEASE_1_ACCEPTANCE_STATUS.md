# Release 1 Acceptance Status

Baseline: `3ccbcc78edefb338c3e0d9742c0cdb6b563b537a`  
Assessment date: 2026-07-14  
Branch: `agent/release-1-completion`

Manual acceptance findings are tracked in `docs/RELEASE_1_TEST_FINDINGS.md`. An
affected criterion cannot be accepted while its linked finding remains open.

This ledger evaluates every bullet in the committed acceptance baseline. “Implemented” describes the current local branch, not production. Release 1 is **not accepted or deployment-ready** until the PostgreSQL, authenticated workflow, backup, timed browser, and production-smoke gates below are completed.

| Baseline line | Acceptance criterion | Status | Evidence / remaining proof |
| ---: | --- | --- | --- |
| 11 | All user-facing NYSA Pocket Ledger text is replaced with NYSA CRM. | Implemented; deployed with approved CORE header | The Pocket Ledger name was removed. The header was subsequently branded NYSA CORE under D-030 and deployed in `1179cca`; technical CRM identifiers remain. |
| 12 | Package description, page title, login, dashboard, server log, and documentation agree. | Implemented; locally verified | `package.json`, `public/index.html`, `public/app.js`, `src/server.js`. Covered by source inspection and the passing syntax/domain/policy/file/dashboard test suite. |
| 13 | The rename does not change production URL, database identity, users, or existing records. | Implemented; integrated test pending | `package.json`, `public/index.html`, `public/app.js`, `src/server.js`. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 17 | An admin can create and deactivate teams. | Implemented; integrated test pending | `004_governance_and_scope.sql`, `governance.js`, `crm-policy.js`, policy tests. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 18 | An admin can assign a manager and internal members to a team. | Implemented locally; CRM Test retest pending | Consolidated User Management now supports direct pending creation, invitations, team assignment, effective-dated role assignments and Admin Assistant team maintenance. R1-UAT-010 remains open pending deployment and explicit user confirmation. |
| 19 | Existing administrator login survives the role migration. | Environment verification pending | `004_governance_and_scope.sql`, `governance.js`, `crm-policy.js`, policy tests. Requires executing migrations on fresh and restored PostgreSQL databases and reconciling retained records. |
| 20 | Each active non-admin user has an explicitly approved role and team mapping. | Implemented locally; CRM Test retest pending | Migration `012` adds effective-dated multi-role assignments with one primary role, team scope, approval evidence and Admin Assistant. External Broker remains interface identity only. See R1-UAT-010. |
| 21 | API tests prove that users cannot access records outside their permitted scope. | Partial; role and Viewer verification pending | Existing policy unit tests pass, but API-enforced Viewer read-only behavior, the new Admin Assistant permission matrix and complete authenticated cross-role denial tests remain open. See R1-UAT-010. |
| 22 | Revoking a user invalidates their active sessions. | Implemented locally; CRM Test retest pending | Separate reasoned Suspend, Reactivate and Revoke actions are implemented and suspension/revocation invalidates active sessions. CRM Test workflow proof remains required. |
| 26 | NYSA Organization Settings are maintained separately from external companies. | Revision 3 deployed to CRM Test; activation/user confirmation pending | R1-AMD-012 Revision 3 adds complete NYSA company-profile/document-default fields, private hashed logo maintenance, Draft -> Approved -> Active -> Retired governance, explained comparison, pre-save logo validation, same-draft retry and reasoned deletion only for unused drafts. Authenticated CRM Test verification confirms v5 is Approved with logo evidence, v1-v4 are Retired protected history, and comparison identifies the logo change from v4 to v5. R1-UAT-011 remains open until activation, remaining proposal-output retest and explicit user confirmation. |
| 27 | An authorized user can create, view, edit, and search a person or external company. | Revision 5 implemented locally; CRM Test retest pending | `004_governance_and_scope.sql`, `governance.js`, `crm.js`, `files-proposals.js` and Customer UI. New-customer lead capture is atomic, the existing-customer dropdown remains alphabetical/searchable, and the readable Customer master now opens against the correct consent schema with customer-originated lead and document actions. CRM Test verification remains required under R1-UAT-018, R1-UAT-021 and R1-UAT-022. |
| 28 | Creating an external company never creates authentication access. | Implemented; integrated test pending | `004_governance_and_scope.sql`, `governance.js`, contact-governance UI. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 29 | One external company can hold multiple business roles without duplication. | Implemented; integrated test pending | `004_governance_and_scope.sql`, `governance.js`, contact-governance UI. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 30 | One contact can hold multiple customer roles without duplication. | Revision 2 implemented locally; CRM Test retest pending | `004_governance_and_scope.sql`, `governance.js`, `crm.js` and contact-governance UI. Atomic new-customer lead capture rolls back the contact role and channels on failure and explains email/phone duplicate matching; authenticated verification remains required under R1-UAT-021. |
| 31 | Phone and email channels preserve raw input and store a validated normalized value. | Implemented; integrated test pending | `004_governance_and_scope.sql`, `governance.js`, contact-governance UI. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 32 | Mobile numbers are normalized to E.164 and emails to lowercase for comparison. | Implemented; locally verified | `004_governance_and_scope.sql`, `governance.js`, contact-governance UI. Covered by source inspection and the passing syntax/domain/policy/file/dashboard test suite. |
| 33 | WhatsApp capability applies only to phone channels and does not duplicate the phone. | Implemented; integrated test pending | `004_governance_and_scope.sql`, `governance.js`, contact-governance UI. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 34 | Screen, API, import, and database rules reject the same invalid channel combinations. | Environment verification pending | `004_governance_and_scope.sql`, `governance.js`, contact-governance UI. Shared validation and constraints are implemented; screen/API/import/database parity must be exercised against PostgreSQL. |
| 35 | Exact phone or email duplicates produce a clear review warning. | Implemented; integrated test pending | `004_governance_and_scope.sql`, `governance.js`, contact-governance UI. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 36 | Merge is authorized, reasoned, audited, and preserves relationship history. | Implemented; integrated test pending | `004_governance_and_scope.sql`, `governance.js`, contact-governance UI. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 37 | Marketing consent cannot become Granted without the exact executed, approved, versioned NYSA Marketing Agreement. | Implemented; integrated test pending | `004_governance_and_scope.sql`, `governance.js`, contact-governance UI. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 39 | Withdrawal, expiry, or supersession updates effective consent and suppresses prohibited marketing communication. | Implemented; integrated test pending | `004_governance_and_scope.sql`, `governance.js`, contact-governance UI. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 41 | Communication preference, documentary consent evidence, and restrictions are visible and enforced. | Implemented; integrated test pending | `004_governance_and_scope.sql`, `governance.js`, contact-governance UI. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 45 | Authorized users can create leads from every approved manual source. | Implemented; integrated test pending | `005_lead_operations.sql`, `009_acceptance_hardening.sql`, `lead-operations.js`, `website-intake.js`. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 46 | Imported leads use stable external identifiers and do not duplicate on retry. | Implemented; integrated test pending | `005_lead_operations.sql`, `009_acceptance_hardening.sql`, `lead-operations.js`, `website-intake.js`. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 47 | A new lead enters a company queue with received timestamp and source. | Revision 3 implemented locally; CRM Test retest pending | Queue-only intake is deployed. New-customer browser capture now commits the customer, lead, initial assignment history and audit evidence atomically and confirms success only after commit. CRM Test proof remains required under R1-UAT-005 and R1-UAT-021. |
| 48 | Routing assigns a team and responsible agent according to configured rules. | Revision 2 implemented locally; CRM Test retest pending | Routing rules select only the Dubai Rental, Off-plan or Secondary Sales team queue, or the company-unassigned fallback. The responsible team lead or Director separately assigns the agent with audit history; routing cannot silently name one. CRM Test proof remains required. |
| 49 | Assignment acceptance, rejection, timeout, and reassignment are timestamped and audited. | Implemented locally; CRM Test retest pending | Repeatable acceptance/first-contact SLA recycling, atomic self-claim and preserved assignment/audit history are implemented. Timed CRM Test proof remains required. |
| 50 | Managers can reassign only within permitted scope unless an admin intervenes. | Amended locally; CRM Test retest pending | Under R1-AMD-007 Revision 2, responsible team leads retain managed-team assignment and Directors have company-wide intervention; Administrator access alone does not confer operational lead-assignment authority. Cross-scope CRM Test proof remains required. |
| 51 | No lead can silently lose company, team, or responsible-agent ownership history. | Implemented; integrated test pending | `005_lead_operations.sql`, `009_acceptance_hardening.sql`, `lead-operations.js`, `website-intake.js`. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 55 | An approved website form creates one company-owned lead with source, campaign, page/form, consent, received time, and customer requirement fields. | Implemented; integrated test pending | `005_lead_operations.sql`, `009_acceptance_hardening.sql`, `lead-operations.js`, `website-intake.js`. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 57 | Invalid authentication, oversized bodies, malformed data, and replayed requests are rejected without partial contact or lead records. | Implemented; integrated test pending | `005_lead_operations.sql`, `009_acceptance_hardening.sql`, `lead-operations.js`, `website-intake.js`. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 59 | Retrying the same accepted website event does not create another contact or lead. | Implemented; integrated test pending | `005_lead_operations.sql`, `009_acceptance_hardening.sql`, `lead-operations.js`, `website-intake.js`. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 60 | Processing failure is visible to authorized support users and can be replayed after correction without bypassing validation or audit. | Implemented locally; CRM Test retest pending | Website intake is consolidated into Audit and Operations with safe metadata and a contextual failed-event replay action; full payloads and secrets remain excluded. |
| 62 | Secrets and full sensitive payloads are absent from browser code, application logs, Git, and user-visible error messages. | Implemented; integrated test pending | `005_lead_operations.sql`, `009_acceptance_hardening.sql`, `lead-operations.js`, `website-intake.js`. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 67 | Business hours and SLA values are configurable. | Implemented locally; CRM Test retest pending | Business-friendly weekday, time and duration controls convert to governed stored minutes without requiring technical interpretation. |
| 68 | Acceptance and first-contact due times are calculated consistently. | Partial; intuitive input and CRM Test reconciliation pending | Calculation tests pass, but business time selectors must convert accurately to the stored values and be reconciled with displayed examples on CRM Test. See R1-UAT-012. |
| 69 | Agent and manager dashboards show approaching and breached SLA records. | Implemented locally; CRM Test retest pending | Breached unattended leads recycle to the routed-team assignment queue and become atomically self-claimable by eligible team agents. |
| 70 | SLA alerts do not duplicate on retry or page refresh. | Implemented; integrated test pending | `005_lead_operations.sql`, `009_acceptance_hardening.sql`, SLA routes/dashboard. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 71 | Reassignment preserves the original SLA and assignment history. | Implemented locally; timed CRM Test proof pending | Each reassignment records a new cycle and deadlines while retaining original deadline fields, superseded assignments, breaches, prior owners and audit history. |
| 72 | Paused/nurture states follow an explicit approved timer policy. | Implemented; integrated test pending | `005_lead_operations.sql`, `009_acceptance_hardening.sql`, SLA routes/dashboard. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 76 | Lead statuses follow the approved lead lifecycle. | Implemented; locally verified | `005_lead_operations.sql`, `009_acceptance_hardening.sql`, `crm.js`, `lead-operations.js`. Covered by source inspection and the passing syntax/domain/policy/file/dashboard test suite. |
| 77 | Invalid status transitions are rejected by the API. | Implemented; locally verified | `005_lead_operations.sql`, `009_acceptance_hardening.sql`, `crm.js`, `lead-operations.js`. Covered by source inspection and the passing syntax/domain/policy/file/dashboard test suite. |
| 78 | Qualified leads can be converted without retyping contact or requirement data. | Implemented; integrated test pending | `005_lead_operations.sql`, `009_acceptance_hardening.sql`, `crm.js`, `lead-operations.js`. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 79 | Lost, unqualified, and duplicate outcomes require approved reasons. | Implemented; integrated test pending | `005_lead_operations.sql`, `009_acceptance_hardening.sql`, `crm.js`, `lead-operations.js`. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 80 | Requirements capture business line, budget, area, property, funding, purpose, and timeline. | Revision 2 implemented locally; CRM Test and AI-provider tests pending | `crm-domain.js`, `crm.js`, `lead-operations.js`, `ai.js`. Budget inputs now normalize uppercase or lowercase K/M/B to exact numeric values before submission; preferred areas prefill Structured requirements and AI drafts still require confirmation. CRM Test verification remains required under R1-UAT-017 and R1-UAT-020. |
| 81 | Requirements can be matched or linked to current inventory without changing the listing. | Implemented locally; CRM Test and AI-provider tests pending | `005_lead_operations.sql`, `009_acceptance_hardening.sql`, `crm.js`, `lead-operations.js`, `ai-domain.js`, `ai.js`. The originating property remains separate from later matching, and each normalized preferred area is evaluated independently from the current structured version. AI only drafts wording from deterministic evidence. CRM Test verification remains required under R1-UAT-019 and R1-UAT-020. |
| 82 | Stage movement and requirement changes remain visible in history. | Implemented; integrated test pending | `005_lead_operations.sql`, `009_acceptance_hardening.sql`, `crm.js`, `lead-operations.js`. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 86 | Users can record calls, notes, meetings, emails, WhatsApp records, and outcomes. | Implemented; integrated test pending | `crm.js`, `009_acceptance_hardening.sql`, activity/task UI. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 87 | Active leads require a next action unless placed in an approved holding/terminal state. | Implemented; integrated test pending | `crm.js`, `009_acceptance_hardening.sql`, activity/task UI. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 88 | Tasks have assignee, due time, priority, status, and completion outcome. | Implemented; integrated test pending | `crm.js`, `009_acceptance_hardening.sql`, activity/task UI. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 89 | Dashboard views separate overdue, due today, upcoming, and completed work. | Implemented; integrated test pending | `crm.js`, `009_acceptance_hardening.sql`, activity/task UI. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 90 | Managers can review team activity without changing the original author or timestamp. | Implemented; integrated test pending | `crm.js`, `009_acceptance_hardening.sql`, activity/task UI. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 91 | Corrections or deletions follow role policy and remain auditable. | Implemented; integrated test pending | `crm.js`, `009_acceptance_hardening.sql`, activity/task UI. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 95 | An authorized administrator can draft, test, approve, and activate a separately versioned qualification model; an active version cannot be edited in place. | Implemented locally; CRM Test retest pending | Lead Qualification Versions now guides Draft -> Test -> Approve -> Activate, governs business-line coverage and uses labelled question cards; approval/activation remain governed. |
| 97 | The approved, versioned model calculates a score and Hot/Warm/Cold result. | Implemented locally; CRM Test retest pending | New leads begin Unassessed; the lead-level Assess qualification popup renders the active model, calculates the result and saves history. Direct routine Hot/Warm/Cold editing is rejected. See R1-UAT-016. |
| 98 | Every result displays the factor contributions and recommended response urgency. | Implemented; locally verified | `006_qualification_and_finance.sql`, qualification routes, domain tests. Covered by source inspection and the passing syntax/domain/policy/file/dashboard test suite. |
| 99 | Authorized users can override the result only with a reason. | Implemented; locally verified | `006_qualification_and_finance.sql`, qualification routes, domain tests. Covered by source inspection and the passing syntax/domain/policy/file/dashboard test suite. |
| 100 | Recalculation creates history rather than rewriting the prior assessment. | Implemented; locally verified | `006_qualification_and_finance.sql`, qualification routes, domain tests. Covered by source inspection and the passing syntax/domain/policy/file/dashboard test suite. |
| 101 | Sensitive traits and social-media conclusions are not scoring inputs. | Implemented; locally verified | `006_qualification_and_finance.sql`, qualification routes, domain tests. Covered by source inspection and the passing syntax/domain/policy/file/dashboard test suite. |
| 102 | Tests cover threshold boundaries, missing inputs, override, and model-version changes. | Implemented; locally verified | `006_qualification_and_finance.sql`, qualification routes, domain tests. Covered by source inspection and the passing syntax/domain/policy/file/dashboard test suite. |
| 103 | Historical assessments retain the exact model version, factor inputs, score, result, override, actor, and time after later model changes. | Implemented; locally verified | `006_qualification_and_finance.sql`, qualification routes, domain tests. Covered by source inspection and the passing syntax/domain/policy/file/dashboard test suite. |
| 108 | Mortgage calculations reproduce independently verified test examples. | Implemented; locally verified | `006_qualification_and_finance.sql`, finance routes, domain tests. Covered by source inspection and the passing syntax/domain/policy/file/dashboard test suite. |
| 109 | Inputs include price, down payment, loan, rate, term, fees, income, and debt where applicable. | Implemented; integrated test pending | `006_qualification_and_finance.sql`, finance routes, domain tests. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 110 | Outputs include estimated monthly payment, total repayment, interest, LTV, and DBR when possible. | Revision 2 implemented locally; CRM Test retest pending | R1-AMD-026 Revision 2 presents EMI, loan/down-payment, total interest, total repayment, upfront cash, LTV and DBR in a review step before saving. DBR explicitly requires monthly income. |
| 111 | ROI outputs distinguish gross yield, net yield, annual costs, vacancy, and cash-on-cash return. | Implemented; locally verified | `006_qualification_and_finance.sql`, finance routes, domain tests. Covered by source inspection and the passing syntax/domain/policy/file/dashboard test suite. |
| 112 | All regulatory and fee assumptions display their version/effective date and are configurable. | Revision 6 deployed; UI workflow user-confirmed, calculation retest pending | Business-labelled rules now support purchase-price, mortgage-value, property-value and quantity bases; percentage, fixed, conditional fixed, percentage-plus-fixed, quantity and estimate ranges; VAT, caps, payer, total treatment, executable transaction/funding/property/service applicability, authority references, placeholders, contextual tests, a closed-by-default form, explicit start/edit/new-version actions, aligned version summaries and governed versions without JSON maintenance. The user confirmed the editing and alignment workflow on CRM Test on 2026-07-16. |
| 113 | Results display an estimate/non-guarantee disclaimer. | Revision 2 implemented locally; CRM Test retest pending | R1-AMD-026 Revision 2 shows the active approved assumption and its disclaimer with the calculated review, then requires explicit indicative-estimate acknowledgement before save. |
| 114 | Calculator scenarios are immutable snapshots linked to the customer and property. | Revision 2 implemented locally; CRM Test retest pending | The workflow calculates without persistence, requires review, and saves only against the exact active assumption version that was previewed. Input changes invalidate the preview; saved snapshots remain immutable. |
| 118 | Authorized listing users can upload supported media within configured limits. | Implemented; integrated test pending | `007_media_documents_proposals.sql`, `private-files.js`, file tests. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 119 | Files are private by default and cannot be guessed through a public URL. | Implemented; locally verified | `007_media_documents_proposals.sql`, `private-files.js`, file tests. Covered by source inspection and the passing syntax/domain/policy/file/dashboard test suite. |
| 120 | File type, size, hash, source, approval status, order, and owner are recorded. | Implemented; locally verified | `007_media_documents_proposals.sql`, `private-files.js`, file tests. Covered by source inspection and the passing syntax/domain/policy/file/dashboard test suite. |
| 121 | Invalid or unauthorized files are rejected without partial business records. | Implemented; locally verified | `007_media_documents_proposals.sql`, `private-files.js`, file tests. Covered by source inspection and the passing syntax/domain/policy/file/dashboard test suite. |
| 122 | Proposal generation includes only approved media. | Revision 13 implemented locally; CRM Test retest pending | The guided builder groups approved media beneath its inventory property, uses authenticated previews, disables it until the property is selected and enforces the active template's per-property limit. Server generation still rejects unapproved or unrelated media. |
| 126 | An agent can select a customer and one property without retyping stored data. | Revision 13 implemented locally; CRM Test retest pending | Proposal builder starts from the lead/customer, current structured requirement, active template and available inventory. Transparent system criteria suggest a shortlist while the user deliberately selects the property; authoritative values are not retyped. |
| 127 | Comparison supports up to three properties. | Implemented locally; CRM Test retest pending | The active template now governs a maximum of three properties; Quick supports one to the configured maximum and Comparison requires at least two. API enforcement and buyer-designer tests pass locally under R1-AMD-006 Revision 7. |
| 128 | Quick, Investment, and Comparison templates apply NYSA branding consistently. | Revision 12 implemented locally; CRM Test retest pending | R1-AMD-012 applies active organization identity and brand evidence. R1-AMD-006 Revision 12 keeps the draft badge clear of the header, uses a simple masked identity reference without customer-facing KYC workflow detail, previews up to three separately numbered matches and displays a unique Inventory ID on every property. Full identity numbers/documents and internal brand-version metadata are excluded. Authenticated CRM Test output proof remains required. |
| 129 | The agent can edit highlights, suitability narrative, assumptions, and approved disclaimers. | Revision 13 implemented locally; CRM Test and AI-provider retest pending | The guided builder lets the user draft and edit highlights, suitability, value rationale and trade-offs after reviewing a deterministic shortlist. It explains assumptions as customer-facing scenario/version context rather than a calculation input and restricts disclaimers to approved values. |
| 130 | Generated output shows property, financial assumptions, agent, date, and version. | Implemented locally; CRM Test retest pending | Proposal output formats the preparation date with the active organization locale/timezone. Exact company-profile, template and brand versions remain in the immutable internal snapshot without exposing internal brand-version metadata in customer-facing output. Unit tests pass; PostgreSQL-backed CRM Test verification remains required under R1-UAT-011. |
| 131 | Financial results in the PDF exactly match the selected saved scenario. | Partial; governed calculation and placeholder verification pending | Immutable saved scenarios exist, but structured fee rules and approved calculated placeholders must be reconciled from the applicable active assumption version through proposal output. See R1-UAT-013. |
| 132 | Developer, organization, customer requirement, property, media, and financial values come from their authoritative modules and are not silently retyped. | Implemented locally; CRM Test retest pending | The template designer uses a curated source catalogue and proposal generation resolves organization, customer, requirement, property, media, scenario, agent and system values from authoritative records into an immutable snapshot. |
| 134 | The agent selects approved images, captions, floor plans, and permitted sections. | Revision 13 implemented locally; CRM Test retest pending | Approved property media is now visibly grouped beneath the corresponding shortlist card with caption and authenticated preview; media and section limits remain template- and server-enforced. |
| 135 | A typical prepared record can produce a reviewed Quick Proposal within three minutes. | Revision 13 implemented locally; timed CRM Test and AI-provider tests pending | One guided workflow exposes readiness, suggests the deterministic shortlist, groups approved media, drafts editable AI narrative, explains saved-scenario assumptions and generates the immutable draft. The authenticated timed browser test remains pending. |
| 136 | Sent proposals cannot be silently overwritten; a change creates a new version. | Implemented; integrated test pending | `007_media_documents_proposals.sql`, `files-proposals.js`, proposal UI/PDF tests. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 137 | A sent version reproduces the exact recipient, data, media, assumptions, template, branding, disclaimer, creator, hash, lead/opportunity, and delivery details. | Partial; governed fee/placeholder reconciliation pending | Immutable version snapshots exist, but the active regulatory/fee rule version and every resolved calculated placeholder must be explicitly retained and reproduced. See R1-UAT-013. |
| 139 | Generated customer PDFs and personal data are excluded from Git and public storage. | Implemented; integrated test pending | `007_media_documents_proposals.sql`, `files-proposals.js`, proposal UI/PDF tests. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 143 | Authorized users can upload supported operational documents to private storage and link an exact version to a contact, lead, activity, listing, proposal, or channel. | Revision 1 implemented locally; CRM Test retest pending | The Customer master now exposes one private immutable document register for direct customer and related-lead documents while preserving scoped download and audit controls. PostgreSQL-backed authenticated workflow verification remains required under R1-UAT-022. |
| 145 | Type, title, direction, classification, status, file metadata, storage reference, hash, version, owner, recipient, and event times are recorded. | Revision 1 implemented locally; CRM Test retest pending | R1-AMD-027 adds the lead document register, maintained type selection, explained direction/classification/status fields, conditional recipient and visible immutable version evidence. |
| 147 | Recording "Offer letter sent" requires a link to the precise sent version. | Implemented; integrated test pending | `007_media_documents_proposals.sql`, `009_acceptance_hardening.sql`, document routes. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 148 | A generated or sent document version cannot be overwritten; revision creates a new version. | Revision 1 implemented locally; CRM Test retest pending | Every uploaded version is marked immutable and Upload new version preserves the prior hash and supersession chain. |
| 149 | File download and metadata access enforce role and record scope and create audit evidence. | Revision 1 implemented locally; security retest pending | The register is lead-scoped; restricted files are limited to their owner or authorized manager, and authenticated downloads retain audit evidence. |
| 150 | Unsupported, oversized, malicious, or unauthorized files fail without partial records. | Implemented; integrated test pending | `007_media_documents_proposals.sql`, `009_acceptance_hardening.sql`, document routes. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 154 | The role-appropriate dashboard is the default authenticated landing screen. | Implemented; integrated test pending | `008_dashboards_reporting.sql`, `dashboards.js`, dashboard UI/domain tests. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 155 | Agent dashboard shows personal assignments, qualification, SLA, next actions, overdue work, exceptions, recent activities, proposal follow-up, and workload. | Rebuilt locally; role acceptance pending | `010_role_dashboard_rebuild.sql`, `dashboard-domain.js`, `dashboards.js`, `dashboard-ui.js`, and role-contract tests. Guarded PostgreSQL fixtures and the manual role checklist are in `DASHBOARD_ROLE_TESTING.md`; authenticated visual and scope-denial proof remains required. |
| 157 | Agents can complete permitted common actions from the dashboard without losing context. | Implemented; integrated test pending | `008_dashboards_reporting.sql`, `dashboards.js`, dashboard UI/domain tests. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 158 | The agent Call Report filters and drills into calls, outcome, duration, related lead/contact, next action, follow-up completion, status, and qualification at call time. | Implemented; integrated test pending | `008_dashboards_reporting.sql`, `dashboards.js`, dashboard UI/domain tests. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 160 | Manager dashboard consolidates effective-dated reporting scope across agents and shows team queue, workload, SLA, aging, calls, activities, follow-up, conversion, proposal, consent, document, data-quality, and integration exceptions. | Rebuilt locally; role acceptance pending | A distinct team workspace and Team to Agent to Lead drill-down are implemented. Manager A/B isolated fixtures are defined; authenticated cross-team denial and visual acceptance remain required. |
| 163 | Managing Director dashboard provides Executive, Sales, Inventory, and Operations and Risk views with company-level current, target, prior-period, trend, and variance context. | Implemented locally; CRM Test retest pending | Dashboard Performance Targets and Alerts uses governed KPI definitions/units, business scope selectors, target numbers, authorized approval and explicit exception rules. |
| 165 | Phase 1 executive leading indicators include lead velocity, source quality, qualification/aging, SLA and follow-up exposure, capacity, proposal workload, inventory readiness, and exception trends; unavailable later-phase financial metrics are explicitly marked unavailable rather than invented. | Partial; integrated acceptance pending | The complete leading-indicator contract and explicit unavailable financial metrics are implemented locally. Migration `010` adds the missing authoritative availability, permit, verification, and portal-readiness fields. PostgreSQL reconciliation and authenticated browser acceptance remain required. |
| 169 | Authorized drill-down follows company to business line to team to manager to agent to underlying record and maintains the selected filters and time period. | Partial; integrated acceptance pending | Full hierarchy and record breadcrumb contracts are implemented locally with contributing-population paths for every executive KPI and leading indicator. Authenticated PostgreSQL scope-denial and browser-path proof remains required. |
| 171 | Management reports cover new leads, movement, activities, lost reasons, conversion, qualification, SLA, workload, proposal, data quality, and current inventory readiness. | Implemented; integrated test pending | `008_dashboards_reporting.sql`, `dashboards.js`, dashboard UI/domain tests. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 173 | Reports filter by date, source, team, agent, business line, and stage as applicable. | Implemented; integrated test pending | `008_dashboards_reporting.sql`, `dashboards.js`, dashboard UI/domain tests. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 174 | All applicable visual components update consistently when a dashboard filter changes. | Implemented; integrated test pending | `008_dashboards_reporting.sql`, `dashboards.js`, dashboard UI/domain tests. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 175 | Every dashboard/report shows data-as-of or last-refresh time and calculation context. | Partial; governed definition maintenance open | Dashboards show calculation context, but administrators currently re-enter an Approved definition as free text. The KPI definition and unit must be system-defined and displayed read-only in business-aware target maintenance. See R1-UAT-009. |
| 176 | Counts reconcile to underlying records for controlled test datasets. | Controlled-data reconciliation pending | `008_dashboards_reporting.sql`, `dashboards.js`, dashboard UI/domain tests. Arithmetic unit tests pass; SQL totals must be reconciled against a seeded PostgreSQL dataset. |
| 177 | Exports respect role scope and create an audit event. | Implemented; integrated test pending | `008_dashboards_reporting.sql`, `dashboards.js`, dashboard UI/domain tests. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 181 | Administrators maintain stable value codes, labels, descriptions, display order, defaults, definition status, effective dates, and replacement mappings. | Implemented locally; CRM Test retest pending | Aligned maintenance, usage impact, draft edit/delete and governed deprecate/retire/replace actions are implemented. |
| 183 | Values already used in records cannot be hard-deleted; retirement preserves history. | Implemented locally; CRM Test retest pending | Only zero-usage drafts can be deleted; used values preserve stable codes, history and retirement evidence. |
| 184 | Label-only changes do not alter stable codes or historical reports. | Implemented locally; integrated proof pending | Lowercase snake_case validation, stable-code immutability after activation/use, usage tracking and consumer mappings are implemented; CRM Test historical-report proof remains required. |
| 185 | Workflow/security value changes require impact review, transition/report mapping, migration where needed, tests, approval, and controlled release. | Implemented; integrated test pending | `004_governance_and_scope.sql`, `009_acceptance_hardening.sql`, governance UI/routes. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 187 | Invalid transitions are rejected consistently by screen, API, import, and database rules. | Environment verification pending | `004_governance_and_scope.sql`, `009_acceptance_hardening.sql`, governance UI/routes. API and database transition guards are implemented; import/screen/API/database parity needs an integrated run. |
| 191 | Every endpoint enforces authentication and role/record scope. | Partial verification | Authentication middleware, CRM policy, private storage, audit calls and migrations. AI routes require internal authentication and `canReadLead` scope; the full authenticated HTTP role matrix remains pending. |
| 192 | No secret, password, session token, customer file, or generated proposal enters Git or logs. | Implemented locally; provider retest pending | The AI credential is environment-only, direct email/phone/identity tokens are redacted, `store:false` is requested, and only safe run metadata is retained. Source inspection and automated tests pass; cPanel configuration and live traffic inspection remain pending. |
| 193 | Material role, consent, document, lead, qualification-model/assessment, proposal, configuration, export, and restricted-file actions are audited. | Revision 3 implemented locally; CRM Test retest pending | R1-AMD-014 Revision 3 adds `AiAssistanceRun` to the governed audit entity constraint so completed provider responses can retain safe audit evidence. PostgreSQL-backed authenticated verification remains required. |
| 195 | Communication restrictions prevent prohibited outreach in the application workflow. | Implemented; integrated test pending | authentication middleware, CRM policy, private storage, audit calls, migrations. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 196 | Production cookies remain Secure, HttpOnly, and appropriately SameSite. | Implemented; integrated test pending | authentication middleware, CRM policy, private storage, audit calls, migrations. Implementation is present; PostgreSQL-backed authenticated workflow verification remains required. |
| 197 | Security headers and request-size limits remain present. | Implemented; locally verified | authentication middleware, CRM policy, private storage, audit calls, migrations. Covered by source inspection and the passing syntax/domain/policy/file/dashboard test suite. |
| 198 | Database queries remain parameterized. | Implemented; locally verified | authentication middleware, CRM policy, private storage, audit calls, migrations. Covered by source inspection and the passing syntax/domain/policy/file/dashboard test suite. |
| 202 | Existing users, listings, comments, audit entries, and sessions migrate without loss. | Environment verification pending | migration runner, local test suite, deployment procedure. Requires executing migrations on fresh and restored PostgreSQL databases and reconciling retained records. |
| 203 | Migration succeeds on an isolated restored production backup. | Environment verification pending | migration runner, local test suite, deployment procedure. Requires executing migrations on fresh and restored PostgreSQL databases and reconciling retained records. |
| 204 | Re-running startup does not reapply an existing migration. | Environment verification pending | migration runner, local test suite, deployment procedure. Requires executing migrations on fresh and restored PostgreSQL databases and reconciling retained records. |
| 205 | A fresh database can apply all migrations from zero. | Environment verification pending | migration runner, local test suite, deployment procedure. Requires executing migrations on fresh and restored PostgreSQL databases and reconciling retained records. |
| 206 | Automated syntax and critical workflow tests pass on the committed source. | Partial verification | All changed JavaScript passes `node --check`; 87 automated tests pass locally, including governed Structured Requirements and blank-limit persistence, Customer/KYC navigation, guided qualification activation, atomic new-customer lead capture, case-insensitive business amounts and buyer-booklet limits. PostgreSQL-backed authenticated CRM Test workflows remain pending. |
| 207 | Dependency audit has no unresolved critical or high production vulnerability. | Verified locally | migration runner, local test suite, deployment procedure. `npm audit --omit=dev --audit-level=high` reports 0 vulnerabilities on 2026-07-14. |
| 208 | A new verified PostgreSQL backup exists before production deployment. | Verified | `nysacrm-pre-release1-20260714.dump` was created before deployment with SHA-256 `176c74b46c839628b1cf1267a089839fc071ad93e7560963c32b5502ebba400c`. |
| 209 | Health, login, existing inventory, lead creation, assignment, activity, calculator, proposal generation, and permissions smoke tests pass in production. | Partial production verification | Health, authenticated dashboard, Leads, Inventory, and Administration loading passed after deployment. Lead creation, assignment, activity, calculator, proposal, and complete role-denial smoke tests remain required. |
| 211 | Project documents and release notes match the deployed Git commit. | Verified | `docs/CURRENT_STATUS.md` and `docs/DEPLOYMENT_HISTORY.md` record deployed source commit `1179cca` and its hotfix ancestry. |
| 215 | External broker access | Out of scope | R1-AMD-011 Revision 3 and D-033 confirm that External Broker is reserved for interface identity only. No external login, session, CRM workspace or record access is included in Release 1. |
| 216 | Customer portal | Out of scope | `docs/ACCEPTANCE_CRITERIA.md` out-of-scope declaration. No Release 1 implementation claim. |
| 217 | Automated social-media screening | Out of scope | `docs/ACCEPTANCE_CRITERIA.md` out-of-scope declaration. No Release 1 implementation claim. |
| 218 | Full opportunity, viewing, offer, negotiation, and deal workflow beyond conversion scaffolding | Out of scope | `docs/ACCEPTANCE_CRITERIA.md` out-of-scope declaration. No Release 1 implementation claim. |
| 219 | Commission settlement | Out of scope | `docs/ACCEPTANCE_CRITERIA.md` out-of-scope declaration. No Release 1 implementation claim. |
| 220 | Full transaction document management | Out of scope | `docs/ACCEPTANCE_CRITERIA.md` out-of-scope declaration. No Release 1 implementation claim. |
| 221 | Live email, Google Calendar, and WhatsApp integration unless separately approved | Out of scope | `docs/ACCEPTANCE_CRITERIA.md` out-of-scope declaration. No Release 1 implementation claim. |
| 222 | Live Property Finder, Bayut, or Meta integration; Release 1 provides the common event/mapping/failure foundation and the NYSA website intake only | Out of scope | `docs/ACCEPTANCE_CRITERIA.md` out-of-scope declaration. No Release 1 implementation claim. |

## Verification recorded

- JavaScript syntax: all repository `.js` files pass `node --check`.
- Automated tests: 94 passed, including Release 1 administration governance, left-menu maintenance navigation, buyer-booklet configuration and limits, contextual fee-rule boundaries, review-before-save financial scenarios with active-assumption readiness, immutable private-document registers and consent separation, lead action-plan ownership, fee-version lifecycle and scenario integration, migration and AI-audit contracts, repeat-SLA queue behavior, configurable proposal inputs, Admin Assistant boundaries, consolidated intake operations, organization-profile logo/retry/cleanup rules, dashboard contracts, scope/security assertions, governed AI REST schema, redaction, evidence and completeness contracts, editable review/apply browser controls, business amount shorthand normalization, model-driven qualification question/access/browser contracts, searchable alphabetical existing-customer selection, business-friendly originating-property terminology, preferred-area normalization/prefill and the user-confirmed 15% typography increase.
- Dependency audit: 0 vulnerabilities at high/critical production threshold.
- Git patch integrity: `git diff --check` passes.
- PDF output: rendered with Poppler and visually inspected for hierarchy, wrapping, footer, page numbering, and clipping.

## Gates still required before acceptance

1. Apply migrations `001`-`017` to a fresh PostgreSQL database and migration `017` to an isolated CRM Test database copy after verifying the already deployed `013`-`016`. Production is not an authorized target for this correction package.
2. Run authenticated CRM Test end-to-end tests for each role and cross-scope denial cases.
3. Seed controlled CRM Test records and reconcile queue, target and proposal results to their audit/history records.
4. Perform the timed Quick Proposal browser acceptance and exact-version download/review/send workflow.
5. Configure the OpenAI credential only in the CRM Test cPanel environment and run all three live AI endpoints, including cross-scope denial, schema, no-mutation, audit-metadata, refusal and timeout tests.
6. Obtain explicit user confirmation for each retested finding before closing it.
