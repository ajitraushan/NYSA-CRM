const $=selector=>document.querySelector(selector);document.querySelectorAll('nav button').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('nav button').forEach(x=>x.classList.toggle('active',x===button));$('#agent').hidden=button.dataset.view!=='agent';$('#admin').hidden=button.dataset.view!=='admin';}));$('#verify').addEventListener('click',()=>{$('#notice').textContent='Synthetic review recorded locally. No file, CRM record or external system was changed.';});$('#task').addEventListener('click',()=>{$('#notice').textContent='Existing CRM Task would be created or reused idempotently. No Task was created in this review.';});
