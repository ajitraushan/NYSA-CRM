const view=document.querySelector('#view'),safe=value=>String(value).replace(/[&<>"']/g,x=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[x]));
const rules=[{label:'Buyer identity document',applies:'Sale · Buyer · Individual',gate:'Before Deal approval',level:'Required',authority:'Private generic document',expiry:'Required · 30 / 14 / 7 / 0 days',status:'Active'},{label:'Seller title evidence',applies:'Sale · Seller · Individual',gate:'Before Close Won',level:'Required',authority:'Release 4 official evidence',expiry:'Required · 14 / 7 / 0 days',status:'Active'},{label:'Tenant address support',applies:'Lease · Tenant · Individual',gate:'Before Deal approval',level:'Advisory',authority:'Private generic document',expiry:'Not tracked',status:'Draft'}];
const requirements=[{party:'Amina Noor',role:'Buyer · Individual',label:'Buyer identity document',gate:'Before Deal approval',state:'Accepted',kind:'accepted',note:'Restricted PDF · immutable version · Manager-reviewed'},{party:'Omar Saleh',role:'Seller · Individual',label:'Seller title evidence',gate:'Before Close Won',state:'Missing',kind:'blocked',note:'Use the existing Release 4 official-document evidence panel'},{party:'Amina Noor',role:'Buyer · Individual',label:'Proof of address',gate:'Before Deal approval',state:'Expiring',kind:'advisory',note:'Expires in 14 calendar days · renewal Task available'}];
function admin(){return`<section class="intro"><h2>Admin maintains the matrix directly</h2><p>Full Administrator drafts, activates, supersedes and retires requirements without a second approver. No legal obligation is seeded by migration.</p></section><div class="kpis"><div><small>Active rules</small><b>2</b></div><div><small>Draft rules</small><b>1</b></div><div><small>Maker-checker</small><b>None</b></div><div><small>External systems</small><b>0</b></div></div><section class="panel"><div class="row"><div><h3>Party-document matrix</h3><span class="muted">Versioned and effective-dated</span></div><button class="primary" data-demo>Draft new requirement</button></div><table><tr><th>Requirement</th><th>Applies to</th><th>Gate</th><th>Evidence / expiry</th><th>Status</th></tr>${rules.map(x=>`<tr><td><b>${safe(x.label)}</b><small>${safe(x.level)}</small></td><td>${safe(x.applies)}</td><td>${safe(x.gate)}</td><td>${safe(x.authority)}<small>${safe(x.expiry)}</small></td><td><span class="pill">${safe(x.status)}</span></td></tr>`).join('')}</table></section><div class="notice"><b>Direct Admin control</b><br>Activating a version freezes it. A later change creates a new version and never rewrites an accepted Deal snapshot.</div>`;}
function deal(){return`<section class="intro"><h2>One combined Deal checklist</h2><p>DEAL-2048 · Residential sale · current parties resolved against the active matrix. The existing completion checklist remains authoritative.</p></section><div class="notice danger"><b>Close Won is not ready</b><br>One required seller document is missing. The expiring buyer document is still accepted and does not block.</div><div class="grid">${requirements.map(x=>`<article class="card ${x.kind}"><div class="row"><div><h3>${safe(x.party)}</h3><small>${safe(x.role)}</small></div><span class="pill">${safe(x.state)}</span></div><p><b>${safe(x.label)}</b><br><span class="muted">${safe(x.gate)}</span></p><small>${safe(x.note)}</small><div class="actions">${x.state==='Missing'?'<button class="primary" data-demo>Create follow-up Task</button>':''}${x.state==='Expiring'?'<button data-demo>Create renewal Task</button>':''}${x.state==='Accepted'?'<button data-demo>Review restricted PDF</button>':''}</div></article>`).join('')}</div><div class="notice"><b>Important:</b> Completing a Task does not accept evidence. Required evidence must be current and accepted before its exact transition.</div>`;}
function customer(){return`<section class="intro"><h2>Customer-scoped compliance view</h2><p>Amina Noor sees only her governed party requirements on accessible Deals. Seller evidence and private files are not exposed here.</p></section><section class="panel"><h3>DEAL-2048 · Amina as Buyer</h3>${requirements.filter(x=>x.party==='Amina Noor').map(x=>`<div class="notice ${x.state==='Accepted'?'success':''}"><div class="row"><b>${safe(x.label)}</b><span class="pill">${safe(x.state)}</span></div><small>${safe(x.gate)} · ${safe(x.note)}</small></div>`).join('')}</section><div class="notice"><b>Restricted metadata only</b><br>No identity number, contact channel, document content, storage key or another transaction party appears in this view.</div>`;}
function tasks(){return`<section class="intro"><h2>Existing My Task Queue</h2><p>Follow-up uses the responsible Deal agent and existing Task lifecycle. There is no parallel compliance queue.</p></section><section class="panel"><table><tr><th>Task</th><th>Safe context</th><th>Assigned</th><th>Due</th><th>Status</th></tr><tr><td><b>Document follow-up — Seller title evidence</b><small>Missing required evidence</small></td><td>Seller · DEAL-2048<small>No private document content</small></td><td>Responsible Deal agent</td><td>Today · High</td><td><span class="pill">Open</span></td></tr><tr><td><b>Document follow-up — Proof of address</b><small>Renewal reminder</small></td><td>Buyer · DEAL-2048<small>Expires in 14 days</small></td><td>Responsible Deal agent</td><td>At reminder point</td><td><span class="pill">Open</span></td></tr></table></section><div class="notice"><b>Manager visibility</b><br>Overdue Tasks remain assigned to the Deal agent and become visible through the maintained reporting scope.</div>`;}
const renders={admin,deal,customer,tasks};function render(name){view.innerHTML=renders[name]();view.querySelectorAll('[data-demo]').forEach(button=>button.onclick=()=>{button.textContent='Review only · no change made';button.disabled=true;});}document.querySelectorAll('nav button').forEach(button=>button.onclick=()=>{document.querySelectorAll('nav button').forEach(x=>x.classList.toggle('active',x===button));render(button.dataset.view);});render('admin');
