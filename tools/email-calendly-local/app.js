document.querySelectorAll('nav button').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('nav button,.view').forEach(item=>item.classList.remove('active'));button.classList.add('active');document.querySelector(`#${button.dataset.view}`).classList.add('active');}));
document.querySelector('[data-demo="email"]').addEventListener('click',()=>{const result=document.querySelector('[data-result="email"]');result.className='result ok';result.innerHTML='<b>Queued safely.</b> Exact draft, authority hash and idempotency key retained; dispatch outcome will be reconciled before the Activity timeline is updated.';});
document.querySelector('[data-demo="meeting"]').addEventListener('click',()=>{const result=document.querySelector('[data-result="meeting"]');result.className='result ok';result.innerHTML='<b>One Meeting Activity projected.</b> Replaying the same synthetic provider event makes no second Activity or calendar event.';});
document.querySelector('[data-demo="viewing"]').addEventListener('click',()=>{const result=document.querySelector('[data-result="viewing"]'),stale=document.querySelector('#stale').checked;result.className=`result ${stale?'blocked':'ok'}`;result.innerHTML=stale?'<b>Blocked safely.</b> Live Inventory is no longer eligible. The booking evidence and task remain visible, but no Viewing was created.':'<b>Viewing created.</b> Broker confirmation, Opportunity version, Match version and live Inventory eligibility all passed in one governed transaction.';});
