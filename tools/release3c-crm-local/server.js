import http from 'node:http';import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';const root=path.dirname(fileURLToPath(import.meta.url)),types={'.html':'text/html; charset=utf-8','.css':'text/css; charset=utf-8','.js':'text/javascript; charset=utf-8'};const server=http.createServer((req,res)=>{if(req.method!=='GET')return void res.writeHead(405).end('GET only');const pathname=new URL(req.url,'http://127.0.0.1').pathname,file=pathname==='/'?'index.html':pathname.slice(1);if(!['index.html','styles.css','app.js'].includes(file))return void res.writeHead(404).end('Not found');res.setHeader('Content-Security-Policy',"default-src 'self'; connect-src 'none'; img-src 'self' data:; style-src 'self'");res.setHeader('X-Content-Type-Options','nosniff');res.setHeader('Cache-Control','no-store');res.writeHead(200,{'Content-Type':types[path.extname(file)]});fs.createReadStream(path.join(root,file)).pipe(res);});server.listen(Number(process.env.PORT||3230),'127.0.0.1',()=>console.log(`Release 3C Gate 3 review: http://127.0.0.1:${server.address().port}/`));
