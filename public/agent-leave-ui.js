(()=>{
  const safe=value=>esc(value),key=()=>crypto.randomUUID(),dateOnly=value=>String(value||'').slice(0,10);
  window.renderMyLeave=async()=>{
    $('#view').innerHTML=`<section class="dashboard-head"><div><div class="eyebrow">NYSA CORE / WORKFORCE</div><h2>My Leave</h2><p>Apply for leave, track Manager decisions and see the balance derived from your governed ledger.</p></div></section><div id="my-leave-workspace" class="loading-state">Loading employment and leave…</div>`;
    const root=$('#my-leave-workspace');let data;try{const[employment,balances,applications]=await Promise.all([api('/crm/my-employment'),api('/crm/my-leave-balances'),api('/crm/my-leave-applications')]);data={employment:employment.employment,balances:balances.balances||[],applications:applications.applications||[]};}catch(error){return root.textContent=error.message;}
    if(!data.employment){root.innerHTML='<div class="empty">No active employment and leave policy is assigned. Contact Administration.</div>';return;}
    root.className='';root.innerHTML=`<section class="stat-grid">${data.balances.map(x=>`<article class="stat-card"><span>${safe(x.label)}</span><strong>${Number(x.availableUnits).toFixed(1)}</strong><small>Available working days · annual allocation ${Number(x.annualUnits).toFixed(1)}</small></article>`).join('')}</section><section class="dashboard-panel"><div class="panel-head"><div><h3>New leave application</h3><small>Saturday and Sunday are excluded. Your reporting Manager is derived from employment; you cannot select the approver.</small></div></div><form id="my-leave-form" class="form-grid"><div><label>Leave type *</label><select name="leaveTypeVersionId" required>${data.balances.map(x=>`<option value="${x.leaveTypeVersionId}">${safe(x.label)} · ${Number(x.availableUnits).toFixed(1)} available</option>`).join('')}</select></div><div><label>Start *</label><input name="startDate" type="date" required></div><div><label>Start portion</label><select name="startPortion"><option value="full">Full day</option><option value="half">Half day</option></select></div><div><label>End *</label><input name="endDate" type="date" required></div><div><label>End portion</label><select name="endPortion"><option value="full">Full day</option><option value="half">Half day</option></select></div><div><label>Supporting evidence reference</label><input name="evidenceReference" placeholder="Reference only; no private content"></div><div class="span3"><label>Reason *</label><textarea name="reason" minlength="5" required></textarea></div><button class="btn btn-primary span3">Create and submit to My Task Queue</button></form></section><section class="dashboard-panel"><div class="panel-head"><div><h3>My applications</h3><small>Every submitted decision retains its employment, policy and Task evidence.</small></div></div><div class="pipeline-table-wrap"><table><tr><th>Reference</th><th>Leave</th><th>Working days</th><th>Approver</th><th>Status</th><th>Action</th></tr>${data.applications.map(x=>`<tr><td>${safe(x.applicationReference)}</td><td><b>${safe(x.leaveTypeLabel)}</b><small>${dateOnly(x.startDate)} to ${dateOnly(x.endDate)}</small></td><td>${Number(x.workingUnits).toFixed(1)}</td><td>${safe(x.approverName||'Assigned on submission')}</td><td><span class="pill">${safe(x.status.replaceAll('_',' '))}</span></td><td>${['draft','submitted'].includes(x.status)?`<button class="btn btn-sm" data-withdraw-leave="${x.id}">Withdraw</button>`:'—'}</td></tr>`).join('')||'<tr><td colspan="6">No leave applications yet.</td></tr>'}</table></div></section>`;
    const form=$('#my-leave-form');form.elements.startDate.value=new Date().toISOString().slice(0,10);form.elements.endDate.value=form.elements.startDate.value;form.addEventListener('submit',async event=>{event.preventDefault();const body=Object.fromEntries(new FormData(form));body.idempotencyKey=key();try{const created=await api('/crm/my-leave-applications',{method:'POST',body});await api(`/crm/my-leave-applications/${created.application.id}/submit`,{method:'POST',body:{idempotencyKey:key()}});toast('Leave submitted to the approver’s My Task Queue');window.renderMyLeave();}catch(error){toast(error.message,9000);}});root.querySelectorAll('[data-withdraw-leave]').forEach(button=>button.onclick=async()=>{if(!confirm('Withdraw this leave application?'))return;try{await api(`/crm/my-leave-applications/${button.dataset.withdrawLeave}/withdraw`,{method:'POST',body:{}});toast('Leave application withdrawn');window.renderMyLeave();}catch(error){toast(error.message,9000);}});
  };

    window.openLeaveReview=async(applicationId,after)=>{let data;try{data=await api(`/crm/leave-applications/${applicationId}/review`);}catch(error){return toast(error.message,9000);}const a=data.application,o=overlay(`<div class="modal lead-modal"><button class="close-x">×</button><div class="detail-head"><div><div class="eyebrow">MY TASK QUEUE · GOVERNED LEAVE DECISION</div><h2>${safe(a.applicationReference)}</h2><p>${safe(a.applicantName)}</p></div><span class="pill">${safe(a.status)}</span></div><section class="proposal-readiness"><b>Leave type: ${safe(a.leaveTypeLabel)}</b><span>Reason for leave: ${safe(a.reason)}</span></section><section class="market-kpis"><div><small>Leave period</small><b>${dateOnly(a.startDate)} – ${dateOnly(a.endDate)}</b></div><div><small>Working days</small><b>${Number(a.workingUnits).toFixed(1)}</b></div><div><small>Available balance</small><b>${Number(data.balance).toFixed(1)}</b></div><div><small>After approval</small><b>${(Number(data.balance)-Number(a.workingUnits)).toFixed(1)}</b></div></section><section class="dashboard-panel"><h3>Assigned-approver evidence</h3><p><b>Evidence reference:</b> ${safe(a.evidenceReference||'Not required / not provided')}</p><small>Leave reason and evidence are visible to the assigned approver here, but not copied to the broad Admin register or team calendar.</small></section><form id="leave-decision-form" class="form-grid"><div><label>Decision *</label><select name="decision"><option value="approve">Approve</option><option value="reject">Reject</option></select></div><div class="span2"><label>Decision reason *</label><input name="reason" minlength="5" required></div><button class="btn btn-primary span3">Record decision and complete Task</button></form></div>`);o.querySelector('#leave-decision-form').onsubmit=async event=>{event.preventDefault();const body=Object.fromEntries(new FormData(event.target));body.version=a.version;body.idempotencyKey=key();try{await api(`/crm/leave-applications/${a.id}/decision`,{method:'POST',body});toast(body.decision==='approve'?'Leave approved and Task completed':'Leave rejected and Task completed');o.remove();after?.();}catch(error){toast(error.message,9000);}};
  };

  window.agentLeaveAdminHTML=()=>`<div class="admin-section agent-leave-admin"><h2>Agent employment and leave</h2><p class="tool-note">Full Administrator maintains employment and leave policy directly. Operational approval stays with the reporting Manager in My Task Queue; Admin status does not grant leave approval.</p><div class="official-document-admin-grid"><form id="leave-type-admin-form" class="form-grid compact-form"><div class="span3"><h3>1. Leave type</h3></div><div><label>Stable code *</label><input name="typeCode" required value="annual_leave"></div><div><label>Label *</label><input name="label" required value="Annual leave"></div><div><label>Classification</label><select name="paidClassification"><option value="paid">Paid</option><option value="unpaid">Unpaid</option></select></div><div><label>Effective from *</label><input name="effectiveFrom" type="date" required></div><label><input name="evidenceRequired" type="checkbox"> Evidence mandatory</label><label><input name="allowNegativeBalance" type="checkbox"> Negative balance permitted</label><div class="span2"><label>Reason *</label><input name="reason" minlength="10" required value="Approved company leave type"></div><button class="btn btn-primary span3">Save and activate leave type</button></form><form id="leave-policy-admin-form" class="form-grid compact-form"><div class="span3"><h3>2. Leave policy and entitlements</h3></div><div><label>Policy code *</label><input name="policyCode" required value="standard_agents"></div><div><label>Name *</label><input name="name" required value="Standard agent leave"></div><div><label>Effective from *</label><input name="effectiveFrom" type="date" required></div><div><label>Decision SLA (working days)</label><input name="decisionDueWorkingDays" type="number" min="1" max="10" value="2"></div><div class="span2"><label>Reason *</label><input name="reason" minlength="10" required value="Approved standard agent policy"></div><div class="span3" id="leave-entitlement-admin">Activate a leave type first.</div><button class="btn btn-primary span3">Save and activate policy</button></form><form id="employment-admin-form" class="form-grid compact-form"><div class="span3"><h3>3. Agent employment</h3></div><div><label>Agent *</label><select name="brokerId" required></select></div><div><label>Reporting Manager *</label><select name="reportingManagerId" required></select></div><div><label>Leave policy *</label><select name="policyVersionId" required></select></div><div><label>Employment start *</label><input name="startDate" type="date" required></div><div><label>Effective from *</label><input name="effectiveFrom" type="date" required></div><div><label>Status</label><select name="employmentStatus"><option value="active">Active</option><option value="leave">On extended leave</option><option value="ended">Ended</option></select></div><div class="span2"><label>Reason *</label><input name="reason" minlength="10" required value="Approved employment maintenance"></div><button class="btn btn-primary span3">Save and activate employment</button></form></div><h3>Employment register</h3><div id="agent-employment-register">Loading…</div><h3>Organization leave register</h3><div id="organization-leave-register">Loading…</div></div>`;

  window.bindAgentLeaveAdministration=async()=>{const typeForm=$('#leave-type-admin-form'),policyForm=$('#leave-policy-admin-form'),employmentForm=$('#employment-admin-form');if(!typeForm)return;const today=new Date().toISOString().slice(0,10);[typeForm,policyForm,employmentForm].forEach(form=>{form.elements.effectiveFrom.value=today;});employmentForm.elements.startDate.value=today;
    const load=async()=>{try{const[types,policies,employments,register]=await Promise.all([api('/admin/leave-types'),api('/admin/leave-policy-versions'),api('/admin/agent-employments'),api('/admin/leave-register')]);const activeTypes=types.leaveTypes.filter(x=>x.status==='active'),activePolicies=policies.policies.filter(x=>x.status==='active');$('#leave-entitlement-admin').innerHTML=activeTypes.length?`<table><tr><th>Include</th><th>Leave type</th><th>Annual days</th><th>Carry forward</th></tr>${activeTypes.map(x=>`<tr><td><input type="checkbox" data-entitlement-type="${x.id}" checked></td><td>${safe(x.label)}</td><td><input type="number" min="0" step=".5" value="30" data-entitlement-annual="${x.id}"></td><td><input type="number" min="0" step=".5" value="0" data-entitlement-carry="${x.id}"></td></tr>`).join('')}</table>`:'Activate a leave type first.';employmentForm.elements.brokerId.innerHTML='<option value="">Select agent</option>'+employments.eligibleAgents.filter(x=>!['director','accountant'].includes(x.jobRole)).map(x=>`<option value="${x.id}">${safe(x.name)} · ${safe(x.jobRole)}</option>`).join('');employmentForm.elements.reportingManagerId.innerHTML='<option value="">Select Manager / Director</option>'+employments.managers.map(x=>`<option value="${x.id}">${safe(x.name)} · ${safe(x.jobRole)}</option>`).join('');employmentForm.elements.policyVersionId.innerHTML='<option value="">Select active policy</option>'+activePolicies.map(x=>`<option value="${x.id}">${safe(x.name)} · v${x.versionNo}</option>`).join('');$('#agent-employment-register').innerHTML=`<table><tr><th>Agent</th><th>Status</th><th>Reports to</th><th>Policy</th><th>Effective</th></tr>${employments.employments.map(x=>`<tr><td>${safe(x.name)}</td><td>${safe(x.employmentStatus||'Not configured')}</td><td>${safe(x.reportingManagerName||'—')}</td><td>${safe(x.policyName||'—')}</td><td>${dateOnly(x.effectiveFrom)||'—'}</td></tr>`).join('')||'<tr><td colspan="5">No employment records.</td></tr>'}</table>`;$('#organization-leave-register').innerHTML=`<table><tr><th>Reference</th><th>Agent</th><th>Leave</th><th>Dates</th><th>Status</th></tr>${register.applications.map(x=>`<tr><td>${safe(x.applicationReference)}</td><td>${safe(x.applicantName)}</td><td>${safe(x.leaveTypeLabel)} · ${Number(x.workingUnits)} day(s)</td><td>${dateOnly(x.startDate)} – ${dateOnly(x.endDate)}</td><td>${safe(x.status)}</td></tr>`).join('')||'<tr><td colspan="5">No applications.</td></tr>'}</table>`;}catch(error){toast(error.message,9000);}};
    typeForm.onsubmit=async event=>{event.preventDefault();const body=Object.fromEntries(new FormData(typeForm));body.evidenceRequired=typeForm.elements.evidenceRequired.checked;body.allowNegativeBalance=typeForm.elements.allowNegativeBalance.checked;try{const result=await api('/admin/leave-type-versions',{method:'POST',body});await api(`/admin/leave-type-versions/${result.leaveTypeVersion.id}/activate`,{method:'POST',body:{}});toast('Leave type activated directly by Administrator');load();}catch(error){toast(error.message,9000);}};
    policyForm.onsubmit=async event=>{event.preventDefault();const body=Object.fromEntries(new FormData(policyForm));body.decisionDueWorkingDays=Number(body.decisionDueWorkingDays);body.entitlements=[...document.querySelectorAll('[data-entitlement-type]:checked')].map(input=>({leaveTypeVersionId:input.dataset.entitlementType,annualUnits:Number(document.querySelector(`[data-entitlement-annual="${input.dataset.entitlementType}"]`).value),carryForwardUnits:Number(document.querySelector(`[data-entitlement-carry="${input.dataset.entitlementType}"]`).value)}));try{const result=await api('/admin/leave-policy-versions',{method:'POST',body});await api(`/admin/leave-policy-versions/${result.policy.id}/activate`,{method:'POST',body:{}});toast('Leave policy activated directly by Administrator');load();}catch(error){toast(error.message,9000);}};
    employmentForm.onsubmit=async event=>{event.preventDefault();const body=Object.fromEntries(new FormData(employmentForm)),brokerId=body.brokerId;delete body.brokerId;body.workPatternCode='mon_fri';try{const result=await api(`/admin/agent-employments/${brokerId}/versions`,{method:'POST',body});await api(`/admin/agent-employment-versions/${result.employmentVersion.id}/activate`,{method:'POST',body:{}});toast('Employment version activated directly by Administrator');load();}catch(error){toast(error.message,9000);}};load();};
})();
